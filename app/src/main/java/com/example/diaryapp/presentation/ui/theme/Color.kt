package com.example.diaryapp.presentation.ui.theme

import androidx.compose.ui.graphics.Color

// ── 배경 ──────────────────────────────────────────
val BackGround = Color(0xFFFAF8F4)

// ── 텍스트 ────────────────────────────────────────
val PrimaryAccent = Color(0xFF1C1B19)
val TextSub1 = Color(0xFF4A4740)
val TextSub2 = Color(0xFF9A968A)
val TextSub3 = Color(0xFFB0AC9F)

// ── 보더/구분선 ───────────────────────────────────
val Border1 = Color(0xFFEDE9DF)
val Border2 = Color(0xFFDEDACF)
val Border3 = Color(0xFFE6E1D4)

// ── 입력창 ────────────────────────────────────────
val InputBackground = Color(0xFFF1EDE4)

// ── 버튼 ──────────────────────────────────────────
val ButtonText = Color(0xFFFAF8F4)
val PressedDarkButton = Color(0xFF0F0E0D)
val DisableButton = Color(0xFFE6E1D4)
val DisableButtonText = Color(0xFF9A968A)

// ── 공통 ──────────────────────────────────────────
val ErrorColor = Color(0xFFE53935)

// ── 하위 호환 alias (기존 코드에서 참조 유지) ─────
val Card = Color(0xFFFAF8F4)
val BoundaryLine = Color(0xFFEDE9DF)
val TextHint = Color(0xFFB0AC9F)
val SearchDrag = Color(0xFFF1EDE4)
